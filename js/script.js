// $ (document).ready (function(){
    function Pizza(types,size,crust,toping)

    this.types = types;
    this.size = size;
    this.crust = crust;
    this.toping = toping;



// });
$("form#newPizza").submit(function(click){
event.preventDefault()
var type = $("#type").val();
var size = $("#size").val();
var crust = $("#crust").val();
var toping = $("#toping").val();

var pizza = new Pizza (type, size, crust, toping);

});

$(document).ready(function(){
    if(type === cheese && size === large && crust === St.Louis)
    return (ksh1000)
    else if(type === cheese && size === medium && crust === stLouis){
        return(ksh800)
    }else if(type === cheese && size === small && crust === stLouis){
        return(ksh600)
    }else if(type === cheese && size === large && crust === neapolitan){
        return(ksh950)
    }else if(type === cheese && size === medium && crust === neapolitan){
        return(ksh750)
    }else if(type === cheese && size === small && crust === neapolitan){
        return(ksh550)
    }else if(type === cheese && size === large && crust === newYork){
        return(ksh)
    }else if(type === cheese && size === medium && crust === newYork){
        return(ksh)
    }else if(type === cheese && size === small && crust === newYork){
        return(ksh)
    }else if(type === cheese && size === large && crust === stuffedCrust){
        return(ksh)
    }else if(type === cheese && size === medium && crust === stuffedCrust){
        return(ksh)
    }else if(type === cheese && size === small && crust === stuffedCrust){
        return(ksh)
    }



    else if(type === veggie && size === large && crust === St.Louis){
    return (ksh1000)
    }else if(type === veggie && size === medium && crust === stLouis){
        return(ksh800)
    }else if(type === veggie && size === small && crust === stLouis){
        return(ksh600)
    }else if(type === veggie && size === large && crust === neapolitan){
        return(ksh950)
    }else if(type === veggie && size === medium && crust === neapolitan){
        return(ksh750)
    }else if(type === veggie && size === small && crust === neapolitan){
        return(ksh550)
    }else if(type === veggie && size === large && crust === newYork){
        return(ksh)
    }else if(type === veggie && size === medium && crust === newYork){
        return(ksh)
    }else if(type === veggie && size === small && crust === newYork){
        return(ksh)
    }else if(type === veggie && size === large && crust === stuffedCrust){
        return(ksh)
    }else if(type === veggie && size === medium && crust === stuffedCrust){
        return(ksh)
    }else if(type === veggie && size === small && crust === stuffedCrust){
        return(ksh)
    }





    // else if(type === cheese && size === large && crust === St.Louis){
    //     return (ksh1000)
    //     }else if(type === cheese && size === medium && crust === stLouis){
    //         return(ksh800)
    //     }else if(type === cheese && size === small && crust === stLouis){
    //         return(ksh600)
    //     }else if(type === cheese && size === large && crust === neapolitan){
    //         return(ksh950)
    //     }else if(type === cheese && size === medium && crust === neapolitan){
    //         return(ksh750)
    //     }else if(type === cheese && size === small && crust === neapolitan){
    //         return(ksh550)
    //     }else if(type === cheese && size === large && crust === newYork){
    //         return(ksh)
    //     }else if(type === cheese && size === medium && crust === newYork){
    //         return(ksh)
    //     }else if(type === cheese && size === small && crust === newYork){
    //         return(ksh)
    //     }else if(type === cheese && size === large && crust === stuffedCrust){
    //         return(ksh)
    //     }else if(type === cheese && size === medium && crust === stuffedCrust){
    //         return(ksh)
    //     }else if(type === cheese && size === small && crust === stuffedCrust){
    //         return(ksh)
    //     }
    
    // else if(type === veggie && size === large && crust === stLouis && topping === extraCheese){
    //     return(ksh900)
    // }else if(type === veggie && size === medium && crust === stLouis && topping === extraCheese){

    // }
});